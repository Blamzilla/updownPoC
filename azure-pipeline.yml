pool:
  name: SelfPool


jobs:
    - job: BuildArchive
      steps:       
        - task: CmdLine@2
          inputs:
           script: |
            ls
            echo hello
        - task: PublishBuildArtifacts@1
          inputs:
            PathtoPublish: '$(Build.SourcesDirectory)\*.zip'
            ArtifactName: 'dev'
            publishLocation: 'Container'